rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    function signedIn() { return request.auth != null; }

    // Check for admin custom claim in auth token
    function isAdmin() {
      return request.auth != null &&
             request.auth.token.admin == true;
    }

    // Subidas del cliente (esto ya te funciona)
    match /user_uploads/{uid}/{docId}/{fileName=**} {
      allow write, read: if signedIn() && request.auth.uid == uid;
    }

    // ✅ Entregables: solo admin puede escribir (production-ready for multiple admins)
    match /deliverables/{docId}/{fileName=**} {
      allow write: if isAdmin();
      allow read: if isAdmin();
    }
  }
}